\* ActorInstanceSharedKey must violate LiveKeysRemain when revoking one instance removes another instance's shared key.
SPECIFICATION Spec
CHECK_DEADLOCK FALSE
CONSTANTS
  Actors <- ModelActors
  Instances <- ModelInstances
  Threads <- ModelThreads
  Subjects <- ModelSubjects
  ActorName = "research"
  RootThread = "root"
  ChildThread = "child"
  AuthorizedInstance <- ModelAuthorizedInstance
  RequestedInstance <- ModelRequestedInstance
  ForeignInstance <- ModelForeignInstance
  ObjectOf <- ModelObjectOf
  KeyOf <- ModelKeyWithoutInstance
INVARIANT LiveKeysRemain
