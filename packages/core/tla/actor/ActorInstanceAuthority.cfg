\* ActorInstanceAuthority must violate AcceptedAuthorized when activation trusts a requested instance.
SPECIFICATION AuthoritySpec
CHECK_DEADLOCK FALSE
CONSTANTS
  Actors <- ModelActors
  Instances <- ModelInstances
  Threads <- ModelThreads
  Subjects <- ModelSubjects
  ActorName = "research"
  RootThread = "root"
  ChildThread = "child"
  AuthorizedInstance <- ModelAuthorizedInstance
  RequestedInstance <- ModelRequestedInstance
  ForeignInstance <- ModelForeignInstance
  ObjectOf <- ModelObjectOf
  KeyOf <- ModelKeyOf
INVARIANT AcceptedAuthorized
