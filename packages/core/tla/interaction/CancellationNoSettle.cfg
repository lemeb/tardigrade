\* Omitting the terminal transition leaves a completed cleanup pending forever. Expected to FAIL CancellationSettles.
SPECIFICATION SpecWithoutSettlement
CHECK_DEADLOCK FALSE
CONSTANTS
  Invocations <- ModelInvocations
  Effects = {e1}
  Calls = {c1}
  Links <- ModelLinks
  RequestIds = {x1, x2}
  Target <- MessageInvocation
INVARIANT TypeOK
PROPERTY CancellationSettles
