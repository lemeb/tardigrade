\* ChildEarly must violate DeliveryFollowsParent when delivery can precede the parent record.
SPECIFICATION EarlySpec
CHECK_DEADLOCK FALSE
CONSTANTS
  Calls <- ModelCalls
  Threads <- ModelThreads
  ProposedTarget <- ModelProposedTarget
  RetryTarget <- ModelRetryTarget
  None = "none"
INVARIANT DeliveryFollowsParent
