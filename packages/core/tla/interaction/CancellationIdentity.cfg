\* Matching a request by id crosses method and epoch boundaries. Expected to FAIL ExactRequestTarget.
SPECIFICATION SpecIdentityLeak
CHECK_DEADLOCK FALSE
CONSTANTS
  Invocations <- ModelInvocations
  Effects = {e1}
  Calls = {c1}
  Links <- ModelLinks
  RequestIds = {x1, x2}
  Target <- MessageInvocation
INVARIANT TypeOK
INVARIANT ExactRequestTarget
