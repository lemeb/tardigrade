\* ChildLive checks that every requested call reaches child initialization under fair delivery.
SPECIFICATION LiveSpec
CHECK_DEADLOCK FALSE
CONSTANTS
  Calls <- ModelCalls
  Threads <- ModelThreads
  ProposedTarget <- ModelProposedTarget
  RetryTarget <- ModelRetryTarget
  None = "none"
PROPERTY AllRequestsSettle
PROPERTY AllChildrenMaterialize
